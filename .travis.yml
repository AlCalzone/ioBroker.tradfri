os:
  - linux
  - osx

#compiler:
#  - gcc
#  - clang

language: node_js
node_js:
  - '4'
  - '6'
  - '7'

before_install:
  - if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then export CC=clang++-3.4; export CXX=clang++-3.4; export CXXFLAGS=-std=c++11; fi
  #- if [[ "$TRAVIS_OS_NAME" == "osx" ]]; then brew unlink pkg-config; fi
  - if [[ "$TRAVIS_OS_NAME" == "linux" ]]; then export CXX=g++-4.8; fi
  
#install:
#  # install specific compiler versions. credit: https://tomassetti.me/continous-integration-on-linux-and-windows-travis-and-appveyor/
#  # g++4.8.1
#  - if [ "$CXX" = "g++" ]; then sudo apt-get install -qq g++-4.8; fi
#  - if [ "$CXX" = "g++" ]; then export CXX="g++-4.8" CC="gcc-4.8"; fi
#  - if [ "$CXX" = "g++" ]; then sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-4.8 90; fi
#  - if [ "$CXX" = "g++" ]; then sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-4.8 90; fi
#  - if [ "$CXX" = "g++" ]; then sudo apt-get install libstdc++-4.8-dev; fi
#  #
#  # clang 3.4
#  - if [ "$CXX" == "clang++" ]; then sudo apt-get install --allow-unauthenticated -qq clang-3.4; fi
#  - if [ "$CXX" == "clang++" ]; then export CXX="clang++-3.4"; fi
  
before_script:
  - npm install winston@2.3.0
  - 'npm install https://github.com/ioBroker/ioBroker.js-controller/tarball/master --production'
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
      - h-rayflood/llvm
    packages:
      - g++-4.8
      - gcc-4.8
      - clang++-3.4
